info:
  name: Deletar Cliente
  type: http
  seq: 5

http:
  method: delete
  url: http://localhost:8080/clientes/1
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
