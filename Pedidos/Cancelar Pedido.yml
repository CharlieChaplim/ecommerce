info:
  name: Cancelar Pedido
  type: http
  seq: 6

http:
  method: delete
  url: http://localhost:8080/pedidos/1
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
