info:
  name: Enviar Pedido
  type: http
  seq: 5

http:
  method: patch
  url: http://localhost:8080/pedidos/1/status
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "ENVIADO"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
