info:
  name: Pagar Pedido
  type: http
  seq: 4

http:
  method: patch
  url: http://localhost:8080/pedidos/1/status
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "PAGO"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
